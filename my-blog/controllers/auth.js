const createNewAccessTokenasync = (req, res) => {
  // get refresh token from client
  //verify refresh token
  //create new access token
  //send to the client
};

module.exports = { createNewAccessTokenasync };
